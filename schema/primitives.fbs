namespace protocol;


table KeyValueObject {
  key:   string (key, required);  // primary key, sorted
  value: [ubyte];                 // arbitrary data
}


table Signature {
  pubkey: [ubyte] (required); // blob (32 bytes)
  sig:    [ubyte] (required); // blob (44 bytes)
}
